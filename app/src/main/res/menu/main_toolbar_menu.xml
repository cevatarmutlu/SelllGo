<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <item
        android:id="@+id/tb_search"
        android:icon="@drawable/ic_tb_search"
        android:title="Search"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="always" />
    <item
        android:id="@+id/tb_add_product"
        android:icon="@drawable/ic_tb_add"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction"
        android:title="@string/ic_tb_add_product" />
    <item
        android:id="@+id/tb_account"
        android:icon="@drawable/ic_tb_account"
        app:showAsAction="always"
        android:title="@string/ic_tb_account"
        />
</menu>